openapi: 3.0.3
info:
  title: Restaurant Service API
  version: 1.0.0
  license:
    name: Proprietary
  description: Current restaurant service stub endpoints.
servers:
  - url: http://restaurant:8080
security: []
paths:
  /metrics:
    get:
      tags: [System]
      summary: Prometheus metrics
      operationId: restaurantMetrics
      responses:
        '200':
          description: Prometheus metrics response
          content:
            text/plain:
              schema:
                type: string

  /:
    get:
      tags: [System]
      summary: Service hello endpoint
      operationId: restaurantHello
      security:
        - BearerAuth: []
      responses:
        '200':
          description: Service response
          content:
            text/plain:
              schema:
                type: string
                example: hello from restaurant service
        '400':
          description: Bad request
          content:
            text/plain:
              schema:
                type: string
        '401':
          $ref: '#/components/responses/Error401'

components:
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    Error401:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'

  schemas:
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
